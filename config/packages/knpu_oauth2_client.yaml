knpu_oauth2_client:
    clients:
        discord:
            type: generic
            provider_class: App\Provider\ExtendedDiscordProvider
            client_id: '%env(DISCORD_APP_ID)%'
            client_secret: '%env(DISCORD_SECRET_KEY)%'
            redirect_route: auth_discord_login

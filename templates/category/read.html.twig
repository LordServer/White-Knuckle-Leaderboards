{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}
    <div class="pt-2 pr-4 pb-2 pl-4">
        <div class="text-xs text-red-900">
            <a href="{{ path('app_index') }}" class="hover:text-red-400 cursor-pointer">Home</a> /
            <a href="{{ path('category_index') }}" class="hover:text-red-400 cursor-pointer">Categories</a> /
            <a href="{{ path('category_read', { 'categoryId': category.id }) }}" class="hover:text-red-400 cursor-pointer">{{ category.name|escape }}</a>
        </div>
        <h1 class="text-4xl font-bold mt-1">{{ category.name|escape }}</h1>
        <div class="mt-2">
            {{ category.rules|escape }}<br><br>
            {{ category.name|escape }} is ranked by {{ category.rankMethod.name|escape }}.<br>
            It has {{ category.subcategory|length }} subcategories; {% for subcategory in category.subcategory %}{{ subcategory.name|escape }}{% if not loop.last %}, {% endif %}{% endfor %}.
        </div>
        {% if is_granted('ROLE_DISCORD_ADMIN') %}
            <div class="text-right pt-2">
                <a href="{{ path('category_update', { 'categoryId': category.id }) }}">
                    <button class="bg-black rounded-sm border border-gray-600 border-solid text-white font-bold p-2 cursor-pointer hover:bg-gray-600">Edit</button>
                </a>
                <a href="{{ path('category_delete', { 'categoryId': category.id }) }}">
                    <button class="bg-red-900 rounded-sm border border-gray-600 border-solid text-white font-bold p-2 cursor-pointer hover:bg-red-400">Delete</button>
                </a>
            </div>
        {% endif %}
    </div>
{% endblock %}

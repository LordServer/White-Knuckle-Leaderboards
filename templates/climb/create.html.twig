{% extends 'base.html.twig' %}

{% block title %}Hello ClimbController!{% endblock %}

{% block body %}
    <div class="pt-2 pr-4 pb-2 pl-4">
        <div class="text-xs text-red-900">
            <a href="{{ path('app_index') }}" class="hover:text-red-400 cursor-pointer">Home</a> /
            <a href="{{ path('climb_index') }}" class="hover:text-red-400 cursor-pointer">Climb</a> /
            <a href="{{ path('climb_create') }}" class="hover:text-red-400 cursor-pointer">New</a>
        </div>
        <h1 class="text-4xl font-bold mt-1">Submit a Climb</h1>
        <div>
            {{ component('ClimbComponent') }}
        </div>
    </div>
{% endblock %}

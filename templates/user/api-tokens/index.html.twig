{% extends 'base.html.twig' %}

{% block title %}Climbers{% endblock %}

{% block body %}
    <div class="pt-2 pr-4 pb-2 pl-4">
        {% include 'partials/breadcrumbs.html.twig' %}
        <h1 class="text-4xl font-bold mt-1">My API Tokens</h1>
        <div>
            <table class="w-full border-collapse">
                <thead>
                    <tr>
                        <th class="text-left">Token</th>
                        <th class="text-left">Expires</th>
                        <th class="text-left">Enabled</th>
                        <th class="text-left">Description</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {% for apiToken in apiTokens %}
                        <tr class="hover:bg-gray-600">
                            <td>{{ apiToken.token|u.truncate(32, '********************')|escape }}</td>
                            <td>{% if apiToken.expiresAt is null %}Never{% else %}{{ apiToken.expiresAt|escape }}{% endif %}</td>
                            <td>{{ apiToken.isEnabled|escape }}</td>
                            <td>{{ apiToken.description|escape }}</td>
                            <td class="text-right">BUTTONS</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
